у меня страница в которой хранится много карточек товара в каждой из них есть блок div с span в нем. span имеет класс .like-hearts через css свойства он окрашен в красный. мне нужно чтобы при нажатии мышкой на него с помощью jquery менялся класс этого дива и я окрашивал его в синий цвет.
  при этом так как пользователь который кликает залогинен на сайте и когда он зайдет на этот сайт через время этот класс должен сохраняться.  
 для этого у меня в есть база данных c названием “site_tw”  в которой есть таблица с названием “fav” в которой есть 3 колонки чтобы сохранить эти данные - id записи, user -тот кто под своим логином и паролем может нажать блок, pet - это карточка которая содержит данные о том какая именно это карточка (много карточек хранятся в базе данных pet и имеют каждая свой id) и в ней хранится этот блок. 
user имеет свое id и находится в другой таблице  “site_tw” этой базы данных (под названием “users”).  сайт использует код php. изменение класса должно быть только если нажимает на него курсором зарегистрированный пользователь который находится в базе данных users.
 “site_tw” база данных подключена к сайту, подключение на странице configs/db.php

еще в карточке товаров есть кнопка с класс i-want-by-buy при нажатии на нее если пользователь не зарегистрирован он должен быть отправлен на страницу http://kp3.test/partials/pages/login.php .   если клиент зарегистрирован, то нужно чтобы в таблице “orders” которая тоже находится в базе данных  “site_tw” была добавлена строка  - в этой таблице 3 колонки id записи, user -тот кто под своим логином и паролем может нажать блок (в этой колонке должен быть поставлен id user), pet - это карточка которая содержит данные о том какая именно это карточка (много карточек хранятся в базе данных pet и имеют каждая свой id) и тут нужно добавить pet_id
вот код карточки товара в которой этот span    

<div class="product-card">
                <div class="foto-of-pet"><img src="/uploads/<?php echo($row['photo']);?>" alt=""></div>
                <span class="title-pet"><p> <?php echo ($row['title']);?></p></span>
                <p class="histopy-of-pet"><?php echo ($row['ad']);?></p>
                <h4 class="price-pet"><?php echo ($row['price']);?></h4>           
                <div class="card-product-down">
                    <span class="like-hearts favorite" data-pet="<?php echo $row['id']; ?>">" ></span>
                    <button class="i-want-by-buy">buy</button>
                    <i><p><?php echo ($row['created']);?></p></i>               
                </div>
            </div>
распиши мне четко какой код размещать на каком файле, где этот файл создавать и как его подключать чтобы все работало

1. Создайте файл Jquery и подключите его на странице с карточками товаров.

<script src="js/jquery.js"></script>

2. В файле Jquery добавьте код для обработки нажатия на элемент с классом .like-heart.

$('.like-hearts').on('click', function(e){
  // получаем pet_id из дата атрибута
  var pet_id = $(this).data('pet');
  // проверяем, авторизован ли пользователь
  if(isAuthenticated()){
   // если да, то изменяем класс элемента 
   $(this).toggleClass('favorite');
   // отправляем запрос в базу данных, чтобы сохранить изменения
   $.ajax({
     url: 'http://kp3.test/scripts/save.php',
     type: 'POST',
     data: {
       pet_id: pet_id
     },
     success: function(data){
       // если все прошло успешно, то выводим сообщение
       console.log('Saved successfully!');
     },
     error: function(err){
       // в случае ошибки выводим ее содержимое
       console.log(err);
     }
   });
  } else {
   // если пользователь не авторизован, то отправляем его на страницу авторизации
   window.location.href = 'http://kp3.test/partials/pages/login.php';
  }
});

3. Создайте файл save.php и поместите туда код для сохранения данных в базу данных.

<?php
// подключаемся к базе данных
include 'configs/db.php';
// получаем pet_id
$pet_id = $_POST['pet_id'];
// проверяем, авторизован ли пользователь
if(isAuthenticated()){
  // получаем id пользователя
  $user_id = getUserId();
  // формируем запрос для добавления данных в базу данных
  $sql = "INSERT INTO fav (user_id, pet_id) VALUES ($user_id, $pet_id)";
  // добавляем данные в базу данных
  $conn->query($sql);
}
?>

4. На странице с карточками товаров добавьте код для проверки авторизован ли пользователь

<?php
// проверяем, авторизован ли пользователь
function isAuthenticated(){
  // если пользователь авторизован, то возвращаем true
  if(isset($_SESSION['user_id']) && $_SESSION['user_id'] != ''){
    return true;
  }
  // иначе возвращаем false
  return false;
}

// получаем id пользователя
function getUserId(){
  return $_SESSION['user_id'];
}
?>

5. На странице с карточками товаров добавьте код для проверки, был ли товар добавлен пользователем в избранное.

<?php
// проверяем, авторизован ли пользователь
if(isAuthenticated()){
  // получаем id пользователя
  $user_id = getUserId();
  // формируем запрос для проверки, был ли товар добавлен пользователем в избранное
  $sql = "SELECT * FROM fav WHERE user_id = $user_id AND pet_id = $pet_id";
  // отправляем запр